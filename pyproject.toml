[build-system]
requires = ["setuptools>=68"]
build-backend = "setuptools.build_meta"

[project]
name = "mc-settings-sync"
dynamic = ["version"]
description = "Apply pre-configured Minecraft settings templates to Prism Launcher instances"
readme = "README.md"
license = { file = "LICENSE" }
requires-python = ">=3.10"
dependencies = []

[project.scripts]
mc-settings-sync = "mc_settings_sync.__main__:main"

[project.optional-dependencies]
dev = ["pytest>=8", "pyinstaller>=6"]

[tool.setuptools.packages.find]
where = ["src"]

# single source of truth, __init__.py owns the version
[tool.setuptools.dynamic]
version = { attr = "mc_settings_sync.__version__" }

[tool.pytest.ini_options]
testpaths = ["tests"]
pythonpath = ["src"]
